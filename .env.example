# --------------------------------------------------
# Versions (use "latest" or a specific tag/version)
# --------------------------------------------------
BITCOIN_VERSION=29.0
LND_VERSION=v0.19.2-beta
ELECTRS_VERSION=v0.10.10
THUNDERHUB_VERSION=v0.14.1
MEMPOOL_BACKEND_VERSION=latest
MEMPOOL_FRONTEND_VERSION=latest

# --------------------------------------------------
# Network (mainnet | testnet | signet | regtest)
# --------------------------------------------------
NETWORK=mainnet

# --------------------------------------------------
# Ports / ZMQ / RPC
# --------------------------------------------------
BITCOIN_RPC_PORT=8332
BITCOIN_P2P_PORT=8333
ZMQ_RAWBLOCK=28332
ZMQ_RAWTX=28333

# RPC auth (used if cookie auth is disabled)
BITCOIN_RPC_USER=btcuser
BITCOIN_RPC_PASSWORD=btcpwd-strong-change-me

# --------------------------------------------------
# Web services
# --------------------------------------------------
# ThunderHub internal port (systemd / localhost)
THUNDERHUB_PORT=3000
# Public port via Nginx reverse proxy
NGINX_TH_PORT=8088
# Enable or disable nginx reverse proxy
USE_NGINX=true

# Mempool
MEMPOOL_BACKEND_PORT=4080

# --------------------------------------------------
# Data directories
# --------------------------------------------------
BITCOIN_DATA_DIR=/data/bitcoin
LND_DATA_DIR=/data/lnd
ELECTRS_DATA_DIR=/data/electrs
MEMPOOL_DATA_DIR=/data/mempool

# --------------------------------------------------
# Bitcoin prune/full mode
# --------------------------------------------------
# These can be preseeded to skip prompts in 10-bitcoin-core.sh
# USE_PRUNE=true
# PRUNE_GB=100

